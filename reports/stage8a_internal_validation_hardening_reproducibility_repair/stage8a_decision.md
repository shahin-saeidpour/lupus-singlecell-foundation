# Stage 8A decision

## Final decision

Stage 8A is complete as an internal-validation hardening and reproducibility repair gate.

Status: `repair_requirements_defined_execution_pending`.

## What was completed

The repository now records:

- the need for one canonical validation protocol;
- reproducibility gaps;
- row-level prediction table schema;
- artifact manifest requirements;
- direct baseline comparison plan;
- manuscript claim gate.

## What remains blocked

The repair is not fully executed because the repository still lacks:

- raw donor embedding artifacts;
- row-level prediction table;
- regenerated canonical CV metrics;
- fold assignment table;
- direct raw or pseudobulk baseline results;
- complete artifact hash manifest.

## Scientific consequence

The current internal result remains promising but not yet manuscript-hardened for strong Q1-style claims.

Q2-style framing may become defensible after canonical metrics, prediction table, artifact hashes, and at least one direct baseline are added.

## Recommended next stage

`Stage 8B — execute canonical internal validation and baseline comparison`.
